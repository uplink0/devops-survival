import React, {useMemo, useState} from 'react';
import {createRoot} from 'react-dom/client';
import {AlertTriangle, CheckCircle2, Terminal, Cpu, MemoryStick, HardDrive, RotateCcw, Skull, ShieldAlert} from 'lucide-react';
import './styles.css';

type Incident={title:string; severity:string; icon:string; description:string; logs:string[]; fix:string; damage:string; points:number};
const incidents:Incident[]=[
 {title:'CrashLoopBackOff',severity:'CRITICAL',icon:'☠',description:'payment-api перезапускается каждые 12 секунд. Прод пока жив, но уже нервничает.',logs:['payment-api | Error: connection refused to postgres:5432','payment-api | retrying in 3s...','kubelet | Back-off restarting failed container'],fix:'Перезапустить PostgreSQL и проверить readinessProbe.',damage:'Удалить deployment payment-api. Потому что почему бы и нет.',points:150},
 {title:'Disk usage 99%',severity:'HIGH',icon:'💾',description:'/var/lib/docker почти закончился. Docker смотрит на тебя с осуждением.',logs:['df -h /var/lib/docker','/dev/sda1  98G  97G  0G  100% /','docker system df','Images: 18.4GB  Containers: 2.1GB'],fix:'Удалить неиспользуемые Docker images/volumes и проверить логи.',damage:'Скачать ещё один 8GB образ ubuntu:latest.',points:120},
 {title:'OOMKilled',severity:'HIGH',icon:'🧠',description:'frontend съел всю память ноды и был культурно убит Kubernetes.',logs:['container frontend | memory usage: 1.98Gi','node | allocatable memory: 2Gi','reason: OOMKilled'],fix:'Найти memory leak или увеличить memory limit после анализа.',damage:'Поставить memory limit 128Mi и сказать «оптимизировали».',points:130},
 {title:'Nginx 502',severity:'MEDIUM',icon:'🌐',description:'Пользователи видят 502. Backend утверждает, что он «у меня работает».',logs:['nginx | connect() failed (111: Connection refused)','upstream: http://10.42.0.17:8000','GET /api/health → 502'],fix:'Проверить Service endpoints и доступность backend pod.',damage:'Перезапустить nginx. Три раза. Авось.',points:100},
 {title:'Certificate expires in 3h',severity:'MEDIUM',icon:'🔐',description:'TLS-сертификат скоро превратится в тыкву.',logs:['openssl x509 -enddate','notAfter=Sep 05 22:17:00 2026 GMT','issuer=Let’s Encrypt'],fix:'Продлить сертификат и проверить автоматическую ротацию.',damage:'Отключить HTTPS «временно».',points:90},
];
function App(){
 const [idx,setIdx]=useState(0),[score,setScore]=useState(0),[health,setHealth]=useState(100),[solved,setSolved]=useState(false),[history,setHistory]=useState<string[]>([]);
 const inc=incidents[idx];
 const stats=useMemo(()=>({cpu:Math.min(98,38+idx*11+(solved?8:0)),ram:Math.min(97,54+idx*7),disk:idx===1?99:61+idx*8,pods:idx===0?'14/15':'15/15'}),[idx,solved]);
 const solve=()=>{if(solved)return; setScore(s=>s+inc.points);setSolved(true);setHistory(h=>[`${inc.title} — RESOLVED +${inc.points}`,...h].slice(0,5));};
 const worsen=()=>{setHealth(h=>Math.max(0,h-20));setHistory(h=>[`Ты сделал хуже: ${inc.title}`,...h].slice(0,5));};
 const next=()=>{setIdx(i=>(i+1)%incidents.length);setSolved(false);};
 return <div className="app"><header><div className="brand"><div className="logo">⌘</div><div><b>DEVOPS SURVIVAL</b><span>production incident simulator</span></div></div><div className="top"><span>HEALTH <strong>{health}%</strong></span><span>SCORE <strong>{score}</strong></span></div></header>
 <main><section className="metrics"><Metric icon={<Cpu/>} label="CPU" value={stats.cpu} unit="%"/><Metric icon={<MemoryStick/>} label="RAM" value={stats.ram} unit="%"/><Metric icon={<HardDrive/>} label="DISK" value={stats.disk} unit="%"/><div className="metric pods"><span className="mi">☸</span><div><small>PODS</small><b>{stats.pods}</b></div></div></section>
 <section className="grid"><div className="incident card"><div className="incident-head"><span className="badge">{inc.severity}</span><span className="id">INC-{String(idx+1).padStart(4,'0')}</span></div><div className="title"><span>{inc.icon}</span><div><h1>{inc.title}</h1><p>{inc.description}</p></div></div><div className="terminal"><div className="term-head"><Terminal size={15}/> incident.log <span>LIVE</span></div>{inc.logs.map((l,i)=><div className="log" key={i}><i>{i+1}</i>{l}</div>)}</div><div className="actions"><button className="fix" onClick={solve} disabled={solved}><CheckCircle2/> {solved?'ПОЧИНЕНО':'ПОЧИНИТЬ'}</button><button className="bad" onClick={worsen}><Skull/> СДЕЛАТЬ ХУЖЕ</button></div>{solved&&<div className="success"><CheckCircle2/> Инцидент закрыт. Production снова делает вид, что всё нормально.</div>}<div className="hint"><ShieldAlert/> <b>Что бы сделал ты?</b> {inc.fix}</div></div>
 <aside><div className="card side"><h3>MISSION CONTROL</h3><div className="mission"><span>Incident</span><b>{idx+1} / {incidents.length}</b></div><div className="bar"><i style={{width:`${((idx+1)/incidents.length)*100}%`}}/></div><button className="next" onClick={next}><RotateCcw/> Следующий инцидент</button></div><div className="card side history"><h3>EVENT LOG</h3>{history.length?history.map((x,i)=><div className="event" key={i}><span>›</span>{x}</div>):<div className="empty">Лог пуст. Пока ты ничего не сломал.</div>}</div><div className="quote">«Это не баг. Это незадокументированная фича.»<small>— любой DevOps в 03:47</small></div></aside></section></main></div>}
function Metric({icon,label,value,unit}:{icon:React.ReactNode;label:string;value:number;unit:string}){return <div className="metric"><span className="mi">{icon}</span><div><small>{label}</small><b>{value}{unit}</b></div><div className="mini"><i style={{width:`${value}%`}}/></div></div>}
createRoot(document.getElementById('root')!).render(<App/>);
